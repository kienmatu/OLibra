# .next and out (Next.js build output) were dropped in the post-cutover
# commit — phase 4 deleted the application that produced them.
node_modules
bun.lock
.artifacts
*.md

# The Laravel side of the repo (Task 2 onward). resources/js is Biome's job
# (biome.json), with different formatting conventions than Prettier's here;
# vendor/, storage/, bootstrap/cache/ and public/build are Composer/PHP
# runtime and build output, not source either stack owns.
resources
vendor
storage
bootstrap/cache
public/build
tsconfig.laravel.json
vite.config.ts
biome.json
components.json
composer.json
composer.lock
phpstan.neon
# pint.json was missing from this list, so `npm run format:check` had been
# failing on it — pre-existing, and not a CI gate (CI's "format check" step
# is vendor/bin/pint --test, which is PHP). Added with its siblings.
pint.json
