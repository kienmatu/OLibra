# Temporary files. Everything except the README that documents the convention.
/.artifacts/*
!/.artifacts/README.md

# Git worktrees live inside the project, never beside it.
/.worktrees/

# A Claude Code worktree, same shape as /.worktrees/ above but a separate
# convention this file did not cover. Found by the QA remediation branch's
# Task 6, whose reported `bun run lint` failure turned out to be 100% of 284
# errors coming from a stray `.claude/worktrees/*/.next/` left by another
# session — unignored, so ESLint (and everything else that walks the tree)
# happily descended into another branch's build output.
/.claude/worktrees/

# Dependencies
/node_modules
.pnp
.pnp.*

# Build output
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# Vercel
.vercel

# Environment. .env.example is committed and documents every variable;
# real values never are.
.env
.env.local
.env.*.local
.env.backup
.env.production

# Test output
/coverage
/playwright-report
/test-results

# Editors and OS
/.fleet
/.idea
/.nova
/.vscode
/.zed
.DS_Store
npm-debug.log*
yarn-error.log*

# Container data. Bind-mounted from compose.yaml so the parish's records
# survive `docker compose down -v` — which also means they must never be
# committed.
/data/
